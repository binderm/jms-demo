group 'com.mrclbndr'
version '1.0.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'war'

sourceCompatibility = 1.8
targetCompatibility = 1.8

war {
    archiveName 'shopping-all.war'
}

task syncWar(type: War, dependsOn: classes) {
    archiveName 'shopping-sync.war'
    rootSpec.exclude('**/BillAvailableListener.class')
    rootSpec.exclude('**/ShippingStateChangedListener.class')
}

task asyncWar(type: War, dependsOn: classes) {
    archiveName 'shopping-async.war'
}

repositories {
    mavenCentral()
}

dependencies {
    compile project(':commons')
    compile 'javax:javaee-api:7.0'
    compile 'org.apache.commons:commons-lang3:3.5'
    compile 'com.fasterxml.jackson.core:jackson-databind:2.8.8.1'
}
